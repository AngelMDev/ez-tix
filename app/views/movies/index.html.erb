<% @movies.each do |movie| %>
  <%= movie.name %>
  <br />
  showings:
  <br />
  <% movie.showings.each do |showing| %>
    <%= link_to showing.time, movie_showing_path(movie.id, showing.id) unless showing.sold_out? %>
  <% end %>
<% end %>

