<!-- todo: remove remote true where not necessary, NOT HERE THO -->
<%= form_for @auditorium, remote: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :capacity %>
  <%= f.text_field :capacity %>
  <%= f.submit "Submit" %>
<% end %>